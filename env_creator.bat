rem  install pipenv
python3 -m pip install pipenv

rem create a new environment
python3 -m pipenv shell

rem install the dependencies into the new environment
python3 -m pip install -r requirements.txt